# With zram, swapping is a compressed memcpy rather than disk IO, so the kernel
# should reach for it eagerly. 130 is above 100 on purpose — permitted since
# kernel 5.8, where the scale was extended for exactly this case.
vm.swappiness = 130
