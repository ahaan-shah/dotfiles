# Use iwd as NetworkManager's wifi driver while NetworkManager keeps ownership
# of the saved profiles.
#
# This pairing is what the taskbar's wifi panel assumes: it talks to nmcli, so
# NM must be the thing that stores credentials. Talking to iwd directly would
# create connections NM does not know about, and the panel would not see them.
[device]
wifi.backend=iwd
